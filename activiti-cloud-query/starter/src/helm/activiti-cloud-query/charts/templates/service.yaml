apiVersion: v1
kind: Service
metadata:
  name: {{ include "activiti-cloud-query.fullname" . }}
  labels:
    {{- include "activiti-cloud-query.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "activiti-cloud-query.selectorLabels" . | nindent 4 }}
