apiVersion: v1
kind: Service
metadata:
  name: {{ include "activiti-cloud-connector.fullname" . }}
  labels:
    {{- include "activiti-cloud-connector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "activiti-cloud-connector.selectorLabels" . | nindent 4 }}
