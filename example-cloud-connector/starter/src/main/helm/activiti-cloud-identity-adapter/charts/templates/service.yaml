apiVersion: v1
kind: Service
metadata:
  name: {{ include "activiti-cloud-identity-adapter.fullname" . }}
  labels:
    {{- include "activiti-cloud-identity-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "activiti-cloud-identity-adapter.selectorLabels" . | nindent 4 }}
