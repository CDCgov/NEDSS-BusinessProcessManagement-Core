apiVersion: v1
kind: Service
metadata:
  name: {{ include "activiti.fullname" . }}
  labels:
    {{- include "activiti.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "activiti.selectorLabels" . | nindent 4 }}
