apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "activiti.fullname" . }}-test-connection"
  labels:
    {{- include "activiti.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "activiti.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
