apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "activiti-cloud-identity-adapter.fullname" . }}-test-connection"
  labels:
    {{- include "activiti-cloud-identity-adapter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "activiti-cloud-identity-adapter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
