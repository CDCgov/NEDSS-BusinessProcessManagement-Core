apiVersion: v1
kind: Service
metadata:
  name: {{ include "modeling.fullname" . }}
  labels:
    {{- include "modeling.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "modeling.selectorLabels" . | nindent 4 }}
