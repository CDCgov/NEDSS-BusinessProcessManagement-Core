apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "modeling.fullname" . }}-test-connection"
  labels:
    {{- include "modeling.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "modeling.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
