apiVersion: v1
kind: Service
metadata:
  name: {{ include "runtime-bundle.fullname" . }}
  labels:
    {{- include "runtime-bundle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "runtime-bundle.selectorLabels" . | nindent 4 }}
