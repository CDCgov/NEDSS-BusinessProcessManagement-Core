apiVersion: v1
kind: Service
metadata:
  name: {{ include "activiti-cloud-runtime-bundle.fullname" . }}
  labels:
    {{- include "activiti-cloud-runtime-bundle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "activiti-cloud-runtime-bundle.selectorLabels" . | nindent 4 }}
