apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nedss-businessprocessmanagement-core.fullname" . }}-test-connection"
  labels:
    {{- include "nedss-businessprocessmanagement-core.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nedss-businessprocessmanagement-core.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
