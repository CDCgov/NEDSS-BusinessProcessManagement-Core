apiVersion: v1
kind: Service
metadata:
  name: {{ include "nedss-businessprocessmanagement-core.fullname" . }}
  labels:
    {{- include "nedss-businessprocessmanagement-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nedss-businessprocessmanagement-core.selectorLabels" . | nindent 4 }}
