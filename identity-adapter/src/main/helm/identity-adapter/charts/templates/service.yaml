apiVersion: v1
kind: Service
metadata:
  name: {{ include "identity-adapter.fullname" . }}
  labels:
    {{- include "identity-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "identity-adapter.selectorLabels" . | nindent 4 }}
